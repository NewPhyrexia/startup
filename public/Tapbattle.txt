title Tapbattle

front-->back:GET user
back-->db:GET user from list

back--> db: (if null recieved) POST new user

back<--db: send user info

front<--back: set user highscore

front-->back:POST user new highscore
back-->db:POST user new highscore